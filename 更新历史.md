# Luo-fe 本地提示词管理器 - 更新历史

## v1.4.8（2026-07-06）

### 改进：从软件分类导入学习样本

学习中心新增"从分类导入"按钮，可将软件已有分类（视角/构图/镜头效果等自定义分类）下的所有提示词一键导入到当前学习类别，自动与已有样本去重，便于快速构建训练样本。

- **新增 `#learning-import-category-btn`**：位于样本编辑区操作栏，点击弹出分类选择列表（`src/index.html`、`src/modules/render.js`）
- **复用 `.batch-move-modal` 样式**：弹出列表显示分类名 + 标签数，点击即导入并关闭（`src/index.html`、`src/style.css`）
- **导入逻辑**：提取 `appState.categories[].prompts[].text`（兼容 string 旧格式），与 textarea 现有内容去重合并，标注"未保存"提醒用户点保存（`src/modules/learning.js`）
- **backdrop click-outside 关闭**：点击弹窗背景可关闭，与取消按钮并列（`src/modules/learning.js`）

### 改进：词典调用诊断增强

针对用户反馈"本地词典没有调用"，从两方面增强可视性：修复 `enabled` 字段缺失误启用学习模型的 bug，并在 chip 与工具栏直观展示各来源命中数量。

- **修复 `enabled` 误判**：`loadLearningSettings` 由 `!== false` 改为 `=== true`，避免 `settings.localLearning` 存在但 `enabled` 字段缺失时被误启用（`main.js`）
- **诊断 IPC 增强**：`tokenizer-diagnostic` 新增返回内存词典加载状态（`dictLoadedInMemory`/`dictTagCount`/`dictUsedFallback`/`dictMetaPath`）与学习设置状态（`learningSettingsEnabled`/`learningSettingsMinConfidence`），并统计自定义规则与学习样本的标签/类别数（`main.js`）
- **诊断面板分区展示**：词典文件 / 词典加载状态（内存）/ 自定义规则 / 学习模型设置 / 学习样本 / 学习模型 六大区块，直观显示"已加载到内存 ✓/✗"、"使用 fallback ✓/✗"、"启用 ✓/✗"（`src/modules/learning.js`）
- **chip 来源徽章**：词典命中 📖、规则命中 ⚙、学习模型 🧠 XX%、未识别"未识别"标签，`.from-dict` 实线左边框 / `.from-rule` 双线左边框（`src/modules/render.js`、`src/style.css`）
- **工具栏来源统计**：`#tokenizer-stats` 显示 `已选 N / 共 T · 📖词典数 · ⚙规则数 · 🧠模型数 · ?未识别数`，各来源带背景色胶囊（`src/modules/render.js`、`src/style.css`）

#### 涉及文件

- **修改**：`main.js`、`src/index.html`、`src/style.css`、`src/modules/render.js`、`src/modules/learning.js`、`package.json`、`更新历史.md`

## v1.4.7（2026-07-06）

### 新功能：本地学习模型辅助分类

新增纯本地、零依赖的机器学习分类模型，用户可上传各类别下的提示词样本，一键训练后模型自动辅助后续导入提示词组的分类决策。所有计算在本地完成，无任何云端依赖。

#### 学习算法

- **多项式朴素贝叶斯**：纯 JS 实现，零 npm 依赖，主进程与渲染进程可共用（`src/modules/learner.js`）
- **特征提取**：整词 + 子词（按下划线拆分）+ 字符 3-gram（带 `^$` 边界标记）
- **拉普拉斯平滑**：α=1.0，避免零概率问题
- **softmax 归一化**：log 后验转概率，减最大值防溢出
- **置信度阈值**：默认 0.6，用户可在设置面板调整（0.4/0.6/0.8/0.9）

#### 分类流程融合

- **4 级匹配优先级**：词典精确匹配 → 词典规范化匹配 → 正则规则匹配 → **学习模型预测（新增 Step 3.5）** → 回退 general/other（`src/modules/classifier-engine.js`）
- **依赖注入**：classifier-engine 通过 `options.predictor` 函数注入 predictTag，保持零依赖，避免循环依赖（`src/modules/classifier-engine.js`）
- **内联解析**：`_resolveLearnedCategoryInline` 内联 learner.js 的 resolveLearnedCategory 逻辑（~10 行），避免 import learner 产生循环依赖（`src/modules/classifier-engine.js`）
- **仅在词典+规则未命中时介入**：学习模型不覆盖已有词典/规则结果，保证既有分类行为不变

#### 学习中心 UI

- **新增 `#learning-modal` 弹窗**：880px 宽，左侧类别管理 + 右侧样本编辑（`src/index.html`、`src/style.css`）
- **类别管理**：左侧侧边栏列出 12 个内置类别（5 大类 + general 下 8 子组）+ 用户自定义类别，支持新建/重命名/删除自定义类别（内置类别不可删改）（`src/modules/learning.js`、`src/modules/render.js`）
- **样本录入**：右侧 textarea 多行粘贴 + `.txt` 文件导入（每行一个标签）（`src/modules/learning.js`）
- **训练状态**：底部状态栏显示"已训练 / 未训练"+ 训练时间 + 样本数 + 类别数（`src/modules/render.js`）
- **一键训练**：`#learning-train-btn` 触发训练，训练后模型自动持久化到 `data/tokenizer/learned-model.json`（`src/modules/learning.js`、`main.js`）
- **删除模型**：`#learning-delete-model-btn` 清除已训练模型（`src/modules/learning.js`）

#### 设置面板

- **新增"学习模型辅助分类"区域**：开关 + 置信度阈值 select + 3 个 info-item（模型状态/样本数/类别数）+ "打开学习中心"按钮（`src/index.html`、`src/modules/settings.js`）
- **非阻塞 IPC 同步**：保存设置时通过 `.catch()` 模式异步同步到主进程，不阻塞 modal 关闭（`src/modules/settings.js`）

#### 词典可靠性增强

- **asar 二级备份**：`src/assets/danbooru-tags-fallback.json` 作为 asar 内备份，主路径 `resources/dictionaries/` 不可读时自动 fallback（`main.js`、`src/assets/danbooru-tags-fallback.json`）
- **诊断面板**：设置面板新增"词典与模型诊断"按钮，点击后显示词典加载状态/asar 备份可用性/自定义规则/学习样本/学习模型状态（`src/index.html`、`src/modules/learning.js`、`main.js`）

#### 主进程 IPC

- **新增 7 个学习相关 IPC handler**：`tokenizer-load-samples`/`tokenizer-save-samples`/`tokenizer-train`/`tokenizer-load-model`/`tokenizer-delete-model`/`tokenizer-diagnostic`/`tokenizer-save-learning-settings`（`main.js`）
- **ESM 动态导入**：主进程通过 `pathToFileURL` + 动态 `import()` 加载 learner.js（`main.js`）
- **classifier IPC 注入**：`tokenizer-classify` IPC 调用时注入 predictor/learnedModel/learningEnabled/minConfidence（`main.js`）

#### State 与持久化

- **新增 `appState.settings.localLearning`**：`{enabled, minConfidence, customCategories, lastTrainedAt, modelVersion, samplesHash}`，深度合并保护（`src/modules/state.js`）
- **运行时缓存**：`appState.learningCache`（samples/model/categories）不参与持久化（`src/modules/state.js`）
- **持久化文件**：`data/tokenizer/learned-samples.json` + `data/tokenizer/learned-model.json`

#### chip 视觉反馈

- **学习模型来源标记**：`.tokenizer-chip.learned` 虚线左边框 + `🧠 XX%` 置信度胶囊（`src/style.css`、`src/modules/render.js`）

#### 涉及文件

- **新增**：`src/modules/learner.js`、`src/modules/learning.js`、`src/assets/danbooru-tags-fallback.json`
- **修改**：`main.js`、`preload.js`、`src/index.html`、`src/style.css`、`src/modules/state.js`、`src/modules/render.js`、`src/modules/app.js`、`src/modules/settings.js`、`src/modules/classifier-engine.js`、`src/modules/tokenizer.js`、`package.json`

#### 非功能要求

- **性能**：单次分类 100 个标签 < 50ms（词典查找 + 规则匹配 + 学习模型预测）
- **离线**：学习与分类全程不发起任何网络请求，CSP `connect-src` 未变更
- **兼容**：现有 localStorage 缓存通过深度合并不被破坏
- **优雅降级**：所有 window.electronAPI 调用使用可选链 `?.`，IPC 未就绪时静默失败
- **可访问性**：modal 沿用 `role="dialog" aria-modal="true"`

## v1.4.6（2026-07-06）

### 新功能：本地分词分类器

新增本地、离线、轻量的分词分类器，自动把外部粘贴的逗号分隔标签（如 Danbooru/Civitai/NovelAI 风格）按语义分组展示，用户在分组结果上即可删除、移动、批量导入到软件已有分类，大幅降低整理成本。

#### 分类引擎（本地词典 + 正则规则）

- **新增模块**：`src/modules/classifier-engine.js`，纯函数分类引擎，导出 `classifyTag`/`classifyTags`/`mergeDictionary`/`CATEGORY_RULES`（`classifier-engine.js`）
- **4 步匹配优先级**：精确匹配 → 规范化匹配（去空格/转下划线/小写）→ 正则规则匹配（10 条）→ 回退 `general/other`（`classifier-engine.js`）
- **内置词典**：`resources/dictionaries/danbooru-tags.json`，245 条标签覆盖人物数量/服装/动作/场景/身体/表情/物品等高频标签，5 大类 + general 下 8 个子组，仅 9.8KB（`resources/dictionaries/danbooru-tags.json`）
- **零依赖**：不引入 ONNX/TFJS 等重型 ML 库，保持极简依赖（项目仍仅 `@fortawesome/fontawesome-free`）

#### 主进程 IPC

- **新增 5 个 IPC handler**：`tokenizer-load-dictionary`/`tokenizer-classify`/`tokenizer-read-dictionary`/`tokenizer-save-custom-rules`/`tokenizer-read-custom-rules`，全部异步 + `{success, ...}` 返回格式（`main.js`）
- **动态 ESM 加载**：主进程通过 `pathToFileURL` + 动态 `import()` 加载 ESM 引擎模块，避免 CJS/ESM 互操作问题（`main.js`）
- **词典懒加载**：分类调用时词典未加载则自动触发加载，首次懒加载不阻塞首屏（`main.js`）
- **开发/生产态路径区分**：`getBuiltinDictionaryPath()` 通过 `app.isPackaged` 区分项目根目录与 `process.resourcesPath`（`main.js`）
- **preload 暴露 5 个 API**：`tokenizerLoadDictionary`/`tokenizerClassify`/`tokenizerReadDictionary`/`tokenizerSaveCustomRules`/`tokenizerReadCustomRules`（`preload.js`）

#### UI 与交互

- **导航栏新增按钮**：与翻译按钮风格一致，图标 `fa-tags`，禁用态置灰不可点击（`index.html`、`style.css`）
- **新增 `#tokenizer-modal` 弹窗**：复用现有 `class="modal"` 模式，含 textarea 输入区 + 结果区 + 4 按钮工具栏（全选/删除选中/移动到分组/导入到分类），宽度 720px，最大高度 85vh（`index.html`、`style.css`）
- **分组展示**：按 5 大类分组，general 内再按 8 个子组二级分组，每组带类别色块 + label + 计数（`render.js`）
- **chip 交互**：圆角胶囊样式，左侧 4px 类别色条；单击主体切换选中态，单击 × 图标直接删除；选中态高亮边框，已导入态淡色勾标不可再选，未识别态"未识别"角标（`style.css`、`render.js`、`tokenizer.js`）
- **新增 `#tokenizer-rules-editor-modal`**：JSON 编辑器 modal，含多行 textarea + 取消/保存按钮，异步加载当前 `custom-rules.json` 内容回填（`index.html`、`tokenizer.js`）

#### 业务流程

- **批量导入到分类**：复用 `data.js:275 batchImportPrompts(targetCategoryId, tags.join('\n'))`，导入成功后 chip 标记 `imported=true` 不消失，顶部通知"已导入 N 个标签到 [分类名]"（`tokenizer.js`）
- **批量删除选中**：通过 `showConfirmDialog` 二次确认（`tokenizer.js`）
- **移动到分组**：仅在 UI 层重组 `appState.tokenizerResults`，不调用 IPC（`tokenizer.js`）
- **目标分类选择**：复用现有 `#batch-move-modal` + `.batch-move-list` 结构（`tokenizer.js`）

#### 设置面板

- **新增"本地分词分类器"区域**：开关、词典信息展示（版本/标签总数/分类数）、编辑自定义规则按钮（`index.html`、`settings.js`）
- **自定义规则编辑**：JSON 编辑器加载当前规则内容，保存时校验 JSON 合法性（含行号定位），保存成功后重新加载词典（`tokenizer.js`、`main.js`）
- **规则合并**：`mergeDictionary` 中 customRules 优先覆盖内置词典同名标签映射，支持用户新增 categories 和 subgroups（`classifier-engine.js`）

#### State 与持久化

- **新增 `appState.settings.localTokenizer`**：`{enabled, customRulesLoaded, lastDictVersion}`，仿 `customLayout` 深度合并保护，旧 localStorage 缓存升级无破坏（`state.js`）
- **运行时缓存**：`appState.tokenizerCache`（词典缓存）和 `appState.tokenizerResults`（分类结果）不参与持久化（`state.js`）

#### 构建配置

- **`build.extraResources`**：追加 `resources/dictionaries/` → `dictionaries/`，打包后词典文件位于 `process.resourcesPath/dictionaries/`（`package.json`）

#### 涉及文件

- **新增**：`src/modules/classifier-engine.js`、`src/modules/tokenizer.js`、`resources/dictionaries/danbooru-tags.json`
- **修改**：`main.js`、`preload.js`、`src/index.html`、`src/style.css`、`src/modules/state.js`、`src/modules/render.js`、`src/modules/app.js`、`src/modules/events.js`、`src/modules/settings.js`、`package.json`

#### 非功能要求

- **性能**：单次分类 100 个标签 < 50ms（纯 JS 字典查找 + 正则匹配）
- **离线**：分类全程不发起任何网络请求，CSP `connect-src` 未变更
- **兼容**：现有 localStorage 缓存通过深度合并不被破坏
- **可访问性**：modal 沿用 `role="dialog" aria-modal="true"`，esc/点背景关闭由现有通用逻辑处理

## v1.4.5（2026-06-26）

### 布局系统重构：比例联动 + 消除滚动条

针对拖拽不跟手、滚动条过多、卡片溢出屏幕、缩放后不自适应等问题，对布局系统进行完整重构，从固定像素改为 flex-grow 比例模型。

#### 拖拽跟手优化

- **移除 RAF 节流**：拖拽过程中不再使用 `requestAnimationFrame` 节流，改为每次 `mousemove` 直接更新 CSS 变量（`events.js`）
- **CSS 变量直更**：拖拽时仅更新 `--layout-{key}-ratio` CSS 变量，浏览器只重算 flex 布局，不触发 JS 对象 reflow，性能开销极低（`events.js`）
- **效果**：拖拽边框完全跟随鼠标，无延迟感

#### 消除全局滚动条

- **容器层 `overflow: hidden`**：`body`、`.main-content`、三个面板（类别/提示词/预览）全部设为 `overflow: hidden`，杜绝外层滚动条（`style.css`）
- **内部滚动区**：预览面板新增 `.preview-scroll-content` 包裹层（`flex: 1; overflow-y: auto`），仅内部内容区可滚动；类别列表和提示词列表保持 `overflow-y: auto` 作为内部滚动区（`index.html`、`style.css`）
- **移除固定高度约束**：删除 `.category-list`、`.prompt-list`、`.preview-panel` 上所有 `max-height: calc(100vh - XXpx)` 约束，改为 `flex: 1` 自适应填充（`style.css`）
- **效果**：全局无滚动条，仅卡片内部可通过鼠标滚轮下滑

#### 卡片横向占满屏幕

- **flex-grow 布局**：三个面板从固定宽度（`width: 195px` / `width: 290px`）改为 `flex: var(--layout-*-ratio, 默认值) 1 0%`，flex-grow 天然将空间分配到 100% 容器宽度（`style.css`）
- **拖拽时自适应**：拖拽时设置 `flex: 0 0 [width]px` 改为直接更新 ratio CSS 变量，相邻面板自动联动调整（`events.js`）
- **效果**：三个卡片始终横向占满软件界面，永不超出屏幕

#### 比例联动 resize

- **数据模型重构**：`panels.{category,prompt,preview}` 从 `{ width, height, order }` 改为 `{ ratio, order }`，默认比例 20:50:30（`state.js`）
- **联动调整算法**：拖拽面板 A 的边框时，仅调整 A.ratio 和相邻面板 B.ratio，二者总和守恒（`totalRatio = startRatioA + startRatioB`），第三个面板不受影响（`events.js`）
- **相邻面板查找**：`_getSortedPanelKeys()` 按 `order` 排序后查找拖拽面板的下一个面板作为联动对象（`events.js`）
- **最小比例保护**：`PANEL_MIN_RATIO_PERCENT = 0.1`，防止单个面板被压缩到不可见（`events.js`）
- **效果**：拖拽一个卡片时另外两个卡片自适应，三个卡片始终横向占满

#### 锁死布局等比例缩放

- **比例持久化**：关闭响应式后记录的是 `ratio` 比例关系而非像素宽度（`state.js`）
- **flex-grow 天然等比缩放**：锁死状态下面板使用 `flex: var(--layout-*-ratio) 1 0%`，软件界面缩放时 flex-grow 自动按比例分配空间，无需 JS 介入（`style.css`）
- **禁用响应式断点**：`body.layout-locked` 类覆盖 1200px / 768px 响应式断点的布局调整（`style.css`）
- **效果**：改变软件界面大小，三个卡片等比例横向缩短或加长，始终占满整个软件界面

#### 涉及文件

- `src/modules/state.js` — `customLayout.panels` 数据模型从 `{ width, height }` 改为 `{ ratio }`，深度合并保护 `direction` 和 `ratio` 字段
- `src/style.css` — 所有面板改用 `flex: var(--layout-*-ratio) 1 0%`，`overflow: hidden` 消除滚动条，新增 `.preview-scroll-content` 内部滚动区
- `src/modules/render.js` — `applyCustomLayout()` 设置 ratio CSS 变量，移除 width/height 变量
- `src/modules/events.js` — 完整重写 resize 逻辑：移除 RAF、CSS 变量直更、比例联动、相邻面板查找
- `src/index.html` — 预览面板内部内容包裹 `.preview-scroll-content`

## v1.4.4（2026-06-26）

### 布局自定义增强与示例组合移除

增强布局自定义功能，支持三栏/单栏切换和三面板独立调大小，并移除示例组合模块。

#### 三栏/单栏切换

- **数据模型**：`customLayout.direction` 新增 `'row'`（三栏，默认）/ `'column'`（单栏）字段（`state.js`）
- **工具栏按钮**：编辑模式工具栏新增"三栏/单栏"切换按钮组，激活态使用主色高亮（`index.html`、`style.css`）
- **切换逻辑**：`_setDirection()` 切换方向后清除面板 inline 尺寸，重新注入对应方向的 resize 手柄（`events.js`）
- **单栏样式**：`body.layout-single-column` 下 `.main-content` 强制 `flex-direction: column`，面板宽度 100%（`style.css`）

#### 三面板均可调大小

- **全面板 resize**：三个面板（类别/提示词/预览）均注入 resize 手柄，不再只有类别和预览（`events.js`）
- **方向感知**：三栏模式手柄为水平方向（调宽度），单栏模式手柄为垂直方向（调高度）（`events.js`、`style.css`）
- **高度存储**：`panels.{category,prompt,preview}.height` 字段存储单栏模式下的高度，`applyCustomLayout` 设置 `--layout-*-height` CSS 变量（`state.js`、`render.js`）
- **保存读取**：`_readLayoutFromDOM()` 根据当前方向读取宽度或高度写回 state（`events.js`）

#### 示例组合移除

- **HTML**：删除 `<div class="examples-section">` 节点（`index.html`）
- **CSS**：删除 `.examples-section`、`.example-item` 等全部样式（`style.css`）
- **JS**：删除 `renderExamples()` 函数、`applyExample()` 函数、`renderExamples()` 调用、`applyExample` 注册（`render.js`、`app.js`）
- **常量**：删除 `EXAMPLES` 常量及其导入（`constants.js`、`app.js`、`render.js`）

#### 升级安装修复

- **修复快捷方式失效**：将删除旧版本程序文件的逻辑从 `customInstall`（文件复制后执行）移至 `customInit`（文件复制前执行），避免删除刚复制的新文件（`installer.nsh`）

## v1.4.3（2026-06-26）

### 布局自定义与升级安装优化

新增主界面卡片布局自定义功能，并改进升级安装流程。

#### 卡片布局自定义

- **新增设置区域**：设置面板"背景设置"下方新增"布局自定义"区域，包含"卡片布局自定义"按钮、"锁死布局"开关、"恢复默认布局"按钮（`index.html`、`settings.js`）
- **数据模型**：`appState.settings.customLayout` 新增 `locked`、`panels.{category,prompt,preview}.{width,order}` 字段，跨重启持久化，深度合并防止部分缓存覆盖默认值（`state.js`）
- **编辑模式**：点击"卡片布局自定义"进入编辑模式，三个面板显示脉冲虚线边框，底部滑入工具栏（保存/取消/恢复默认）（`style.css`、`events.js`）
- **拖拽换位**：每个面板顶部注入蓝色手柄，拖拽到另一面板上释放即可交换位置，使用 FLIP 动画实现平滑过渡（`events.js`、`style.css`）
- **拖拽边框调整大小**：类别卡片和预览卡片右侧注入 resize 手柄，拖拽调整宽度（140-400px / 200-600px），提示词卡片保持 `flex:1` 自适应（`events.js`、`style.css`）
- **应用布局**：`applyCustomLayout()` 通过 CSS 变量（`--layout-category-width` 等）和 `order` 属性应用布局，`body.layout-locked` 类切换锁死状态（`render.js`、`style.css`）

#### 锁死布局

- **禁用响应式**：`body.layout-locked` 类下 `.main-content` 强制 `flex-direction: row`、`flex-wrap: nowrap`，覆盖 1200px / 768px 响应式断点的布局调整（`style.css`）
- **固定面板宽度**：锁死状态下类别面板 `flex: 0 0 [width]px`，预览面板 `flex: 0 0 [width]px`，提示词面板 `flex: 0 1 [width]px`，最小宽度保证可读性（`style.css`）

#### 恢复默认布局

- **一键重置**：`resetLayoutToDefault()` 重置 `customLayout` 到默认值，清除所有面板 inline 样式和 CSS 变量，解除锁死布局（`events.js`）

#### 升级安装优化

- **检测旧安装位置**：`customInit` 读取 `HKCU`/`HKLM` 的 `InstallLocation` 注册表项，将 `$INSTDIR` 默认设为旧安装目录（`installer.nsh`）
- **保留数据删除旧程序**：`customInstall` 在复制新文件前删除旧版本程序本体（`*.exe`、`*.dll`、`*.pak`、`locales/`、`resources/`），严格保留 `data/` 文件夹（`installer.nsh`）

#### UI 动画

- **脉冲边框**：`@keyframes pulse-border` 2s ease-in-out 无限循环，仅动画 `outline-color` 和 `box-shadow`，不触碰 layout 属性（`style.css`）
- **手柄渐入**：`@keyframes handle-appear` 280ms cubic-bezier(0.16, 1, 0.3, 1)，opacity + translateY（`style.css`）
- **工具栏滑入**：`.layout-edit-toolbar` 320ms cubic-bezier(0.16, 1, 0.3, 1)，transform + opacity（`style.css`）
- **FLIP 换位动画**：250ms cubic-bezier(0.22, 1, 0.36, 1)，仅 transition `transform`（`style.css`、`events.js`）

## v1.4.2（2026-06-26）

### 交互优化

针对提示词选中与批量操作的视觉冗余和交互冲突进行优化。

#### 视觉简化

- **移除选中卡片右上角对勾**：`.prompt-item.selected::after` 规则删除，选中状态仅由左侧复选框 + 靛蓝边框/背景表示；`.prompt-item` 的 `padding-right` 从 `1.5rem` 恢复为 `0.75rem`（`style.css`）

#### 批量模式交互重构

- **移除双复选框**：批量模式下不再显示 `.prompt-checkbox`（选用）和 `.batch-checkbox`（批量）两个复选框，改为点击卡片任意位置直接切换批量选中（`render.js`、`style.css`）
- **红色高亮标识**：新增 `.prompt-item.batch-selected` 样式，复用现有 `--danger-color: #f43f5e` 色板（新增 `--danger-light`、`--danger-glow` 变量），与正常选用的靛蓝形成清晰对比（`style.css`）
- **保留选用状态**：进入批量模式时 `appState.selectedPrompts` 数据不变，仅渲染层不显示选用状态；退出后恢复正常显示，批量删除/移动操作会自动同步更新选用状态（`render.js`）
- **全选/取消全选适配**：`selectAllPrompts()` / `deselectAllPrompts()` 在 `batchMode` 下操作 `appState.batchSelected` 而非 `selectedPrompts`（`data.js`）
- **隐藏常用提示词**：批量模式下 `.frequent-section` 隐藏，避免误触选用（`style.css`）

#### 涉及文件

- `src/style.css` — 删除 `::after` 对勾、新增 `--danger-light`/`--danger-glow` 变量、新增 `.batch-selected` 样式、批量模式 CSS 调整
- `src/modules/render.js` — `renderPromptList()` 按 `appState.batchMode` 分支渲染
- `src/modules/data.js` — `selectAllPrompts()` / `deselectAllPrompts()` 适配批量模式

---

## v1.4.1（2026-06-24）

### 性能优化

全面性能优化，降低运行时内存占用和 CPU 使用率，所有功能保持不变。

#### 内存优化

- **LRU 图片缓存**：提示词图片缓存（`_imageCache`）从无上限 Map 改为 LRU 策略（最大 20 条），消除长时间使用后的内存泄漏（`render.js`）
- **防抖保存使用频率**：`savePromptUsage()` 改为防抖调用（`debounce`），连续使用多个提示词时 I/O 次数从 N 降至 1（`state.js`）

#### CPU 优化

- **缓存常用提示词计算**：`getFrequentPrompts()` 结果缓存，仅在分类或请求数量变化时重新计算，避免每次渲染 O(n) 扫描（`state.js`）
- **缓存快捷键解析**：`parseShortcut()` 结果缓存，快捷键字符串未变时不重复解析（`app.js`）
- **按需注册鼠标事件**：面板拖拽的 `mousemove`/`mouseup` 监听器从全局常驻改为在 `mousedown` 时注册、`mouseup` 时注销，非拖拽时零开销（`events.js`）
- **搜索短路优化**：搜索时缓存 `text.toLowerCase()` 结果避免重复调用；text 已匹配时跳过 translation 检查（`search.js`）

#### GPU/渲染优化

- **离屏渲染跳过**：为 `.category-list`、`.prompt-list`、`.selected-prompts` 添加 `content-visibility: auto`，浏览器自动跳过离屏元素渲染（`style.css`）
- **面板渲染隔离**：为 `.category-panel`、`.prompt-panel`、`.preview-panel` 添加 `contain: layout paint style`，隔离布局计算和绘制范围（`style.css`）

### 构建改进

- 新增便携版单 exe 构建目标（`portable`）
- 构建脚本拆分：`build:nsis`、`build:portable`、`build:zip`、`build:all`
- 产出三个版本：安装版 exe、便携版单 exe、便携版 zip 解压版

---

## v1.4（2026-06-23）

### 严重问题修复

#### 1. 软件卡死问题全面修复

**根因**：主进程中大量使用同步文件系统操作（`fs.*Sync()`），阻塞了 Electron 主进程事件循环，导致所有窗口和 IPC 响应冻结。

**修复内容**：

- **提示词图片处理器**（5个 IPC 处理器）
  - `save-prompt-image`：`fs.writeFileSync` → `await fs.promises.writeFile`
  - `read-prompt-image`：`fs.readFileSync` → `await fs.promises.readFile`（移除 `existsSync` 前置检查，改用 try/catch）
  - `delete-prompt-image`：`fs.unlinkSync` → `await fs.promises.unlink`
  - `rename-prompt-image`：`fs.renameSync` → `await fs.promises.rename`
  - `delete-category-images`：`fs.readdirSync` + `forEach` + `unlinkSync` → `await fs.promises.readdir` + `Promise.all` 并行删除

- **缓存处理器**（5个 IPC 处理器）— 异步 + 性能优化
  - `cache-read`：移除双重 JSON.parse/stringify 和 MD5 哈希校验（本地缓存无需完整性校验，原逻辑每次读取做两次 JSON 解析 + 一次 MD5，大缓存时严重阻塞）
  - `cache-write`：移除 `rawJson` 和 `hash` 字段（原逻辑缓存文件大小翻倍 — 同时存 `data` 和 `rawJson`）
  - `cache-delete`：改异步
  - `cache-clear`：`forEach` + `unlinkSync` → `Promise.all` 并行删除
  - `cache-info`：递归同步目录遍历 → 异步递归 + `Promise.all` 并行统计（原逻辑同步递归遍历整个数据目录，文件多时长时间阻塞）

- **导出文件**
  - `save-export-file`：`fs.writeFileSync` → `await fs.promises.writeFile`

- **数据迁移函数**（3个）— 启动时阻塞
  - `copyDirRecursive` → `copyDirRecursiveAsync`：递归同步 → 异步 + `Promise.all` 并行复制
  - `migrateDataToNewDir` → `migrateDataToNewDirAsync`：同步递归移动 → 异步
  - `migrateUserDataIfNeeded` → `migrateUserDataIfNeededAsync`：启动时同步迁移（阻塞窗口创建）→ 异步，`app.whenReady` 中 `await` 调用

- **清理**
  - 移除不再使用的 `crypto` 模块导入和 `computeHash` 函数
  - 删除死代码 `migrateDirContents`（从未被外部调用）

#### 2. 数据目录嵌套问题修复

**问题**：安装多次后，数据目录中出现无限递归嵌套的文件夹结构（`data\Luo-fe提示词管理器\Luo-fe提示词管理器\...\Luo-fe提示词管理器`），导致 Windows 路径长度超限，无法删除。

**根因**：当用户选择的新数据目录位于旧数据目录内部时，`migrateDataToNewDirAsync` 会遍历旧目录的所有子项，其中包含新目录本身，导致 `copyDirRecursiveAsync` 把新目录复制到新目录内部，产生无限递归嵌套，直到 Windows 路径长度超限（260 字符）。

**修复内容**：

- 新增 `isPathInside(target, source)` 工具函数：检测目标路径是否在源路径内部
- `copyDirRecursiveAsync`：添加安全检查，当 `dest` 在 `src` 内部时拒绝复制（防止递归嵌套的最终防线）
- `migrateDataToNewDirAsync`：遍历旧目录时跳过新目录本身（当新目录在旧目录内部时）
- `migrateDataToNewDirAsync`：当新目录在旧目录内部时，不删除旧目录（避免删除新目录）
- `migrateUserDataIfNeededAsync`：跳过作为新数据目录祖先的旧目录
- `set-data-directory` IPC 处理器：添加前置验证，禁止将数据目录设置到当前数据目录的子目录内

### 新功能

#### 1. 安装时读取之前的数据存储地址

- NSIS 安装程序 `customInit` 宏读取旧版本安装目录（从注册表 `InstallLocation`）
- `customInstall` 宏将旧安装目录写入 `old-install-dir.txt` 标记文件（UTF-16LE 编码）
- 应用启动时 `migrateUserDataIfNeededAsync` 读取标记文件，将旧安装目录下 `data` 文件夹的数据迁移到当前数据目录

#### 2. 设置中选择数据地址

- 设置面板新增数据目录管理功能：
  - 显示当前数据存储位置
  - 更改数据存储位置（选择新目录后自动迁移数据）
  - 打开数据目录（在文件资源管理器中打开）
  - 重置为默认位置

### NSIS 安装程序改进

- `customInit`：强制关闭可能正在运行的应用进程（`taskkill`，正确处理中文文件名）
- `customInit`：删除旧版本注册表项，跳过 `uninstallOldVersion`（避免旧卸载程序的 `CHECK_APP_RUNNING` 因 cmd.exe 编码问题误报进程正在运行）
- `customInstall`：确保安装目录下存在 `data` 目录
- `customUnInstall`：卸载前关闭应用进程，不删除任何用户数据
- `!ifndef BUILD_UNINSTALLER` 守卫：解决 `Var OldInstallDir` 在卸载程序构建中未引用导致的编译错误

### 构建系统改进

- 简化构建脚本：
  - `build:nsis`：仅构建 NSIS 安装版
  - `build:portable`：构建便携版（dir + zip）
  - `build:all`：同时构建安装版和便携版
- 移除 `portable` 单 exe 目标，改用 `dir` + zip 方式生成便携版
- 清理项目结构，移除所有辅助脚本和旧构建输出

### 项目文件清理

删除了以下非核心文件：
- `build_with_icon.js`、`create_ico.js`、`set_icon.js` — 图标辅助脚本
- `check_icons.ps1`、`analyze_ico.ps1` — 图标检查脚本
- `debug_app.js` — 调试脚本
- `README.md`、`便携版测试报告与性能优化说明.md` — 旧文档
- `src/splash.html` — 未使用的启动画面文件（启动画面已内嵌在 main.js 中）

---

## v1.3

### 功能改进

- 优化启动速度，添加启动画面（Splash Window）
- 添加窗口状态记忆（位置、大小、最大化状态）
- 优化图标处理逻辑，验证 ICO 文件格式有效性

---

## v1.2

### 功能改进

- 添加背景图片功能
- 添加提示词预览图片功能
- 添加数据导出/导入功能
- 添加缓存系统提升加载速度

---

## v1.1

### 初始功能

- 提示词分类管理
- 提示词增删改查
- 批量操作
- 搜索功能
- 翻译功能
- 界面主题设置
