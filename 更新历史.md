# Luo-fe 本地提示词管理器 - 更新历史

## v1.4.1（2026-06-24）

### 性能优化

全面性能优化，降低运行时内存占用和 CPU 使用率，所有功能保持不变。

#### 内存优化

- **LRU 图片缓存**：提示词图片缓存（`_imageCache`）从无上限 Map 改为 LRU 策略（最大 20 条），消除长时间使用后的内存泄漏（`render.js`）
- **防抖保存使用频率**：`savePromptUsage()` 改为防抖调用（`debounce`），连续使用多个提示词时 I/O 次数从 N 降至 1（`state.js`）

#### CPU 优化

- **缓存常用提示词计算**：`getFrequentPrompts()` 结果缓存，仅在分类或请求数量变化时重新计算，避免每次渲染 O(n) 扫描（`state.js`）
- **缓存快捷键解析**：`parseShortcut()` 结果缓存，快捷键字符串未变时不重复解析（`app.js`）
- **按需注册鼠标事件**：面板拖拽的 `mousemove`/`mouseup` 监听器从全局常驻改为在 `mousedown` 时注册、`mouseup` 时注销，非拖拽时零开销（`events.js`）
- **搜索短路优化**：搜索时缓存 `text.toLowerCase()` 结果避免重复调用；text 已匹配时跳过 translation 检查（`search.js`）

#### GPU/渲染优化

- **离屏渲染跳过**：为 `.category-list`、`.prompt-list`、`.selected-prompts` 添加 `content-visibility: auto`，浏览器自动跳过离屏元素渲染（`style.css`）
- **面板渲染隔离**：为 `.category-panel`、`.prompt-panel`、`.preview-panel` 添加 `contain: layout paint style`，隔离布局计算和绘制范围（`style.css`）

### 构建改进

- 新增便携版单 exe 构建目标（`portable`）
- 构建脚本拆分：`build:nsis`、`build:portable`、`build:zip`、`build:all`
- 产出三个版本：安装版 exe、便携版单 exe、便携版 zip 解压版

---

## v1.4（2026-06-23）

### 严重问题修复

#### 1. 软件卡死问题全面修复

**根因**：主进程中大量使用同步文件系统操作（`fs.*Sync()`），阻塞了 Electron 主进程事件循环，导致所有窗口和 IPC 响应冻结。

**修复内容**：

- **提示词图片处理器**（5个 IPC 处理器）
  - `save-prompt-image`：`fs.writeFileSync` → `await fs.promises.writeFile`
  - `read-prompt-image`：`fs.readFileSync` → `await fs.promises.readFile`（移除 `existsSync` 前置检查，改用 try/catch）
  - `delete-prompt-image`：`fs.unlinkSync` → `await fs.promises.unlink`
  - `rename-prompt-image`：`fs.renameSync` → `await fs.promises.rename`
  - `delete-category-images`：`fs.readdirSync` + `forEach` + `unlinkSync` → `await fs.promises.readdir` + `Promise.all` 并行删除

- **缓存处理器**（5个 IPC 处理器）— 异步 + 性能优化
  - `cache-read`：移除双重 JSON.parse/stringify 和 MD5 哈希校验（本地缓存无需完整性校验，原逻辑每次读取做两次 JSON 解析 + 一次 MD5，大缓存时严重阻塞）
  - `cache-write`：移除 `rawJson` 和 `hash` 字段（原逻辑缓存文件大小翻倍 — 同时存 `data` 和 `rawJson`）
  - `cache-delete`：改异步
  - `cache-clear`：`forEach` + `unlinkSync` → `Promise.all` 并行删除
  - `cache-info`：递归同步目录遍历 → 异步递归 + `Promise.all` 并行统计（原逻辑同步递归遍历整个数据目录，文件多时长时间阻塞）

- **导出文件**
  - `save-export-file`：`fs.writeFileSync` → `await fs.promises.writeFile`

- **数据迁移函数**（3个）— 启动时阻塞
  - `copyDirRecursive` → `copyDirRecursiveAsync`：递归同步 → 异步 + `Promise.all` 并行复制
  - `migrateDataToNewDir` → `migrateDataToNewDirAsync`：同步递归移动 → 异步
  - `migrateUserDataIfNeeded` → `migrateUserDataIfNeededAsync`：启动时同步迁移（阻塞窗口创建）→ 异步，`app.whenReady` 中 `await` 调用

- **清理**
  - 移除不再使用的 `crypto` 模块导入和 `computeHash` 函数
  - 删除死代码 `migrateDirContents`（从未被外部调用）

#### 2. 数据目录嵌套问题修复

**问题**：安装多次后，数据目录中出现无限递归嵌套的文件夹结构（`data\Luo-fe提示词管理器\Luo-fe提示词管理器\...\Luo-fe提示词管理器`），导致 Windows 路径长度超限，无法删除。

**根因**：当用户选择的新数据目录位于旧数据目录内部时，`migrateDataToNewDirAsync` 会遍历旧目录的所有子项，其中包含新目录本身，导致 `copyDirRecursiveAsync` 把新目录复制到新目录内部，产生无限递归嵌套，直到 Windows 路径长度超限（260 字符）。

**修复内容**：

- 新增 `isPathInside(target, source)` 工具函数：检测目标路径是否在源路径内部
- `copyDirRecursiveAsync`：添加安全检查，当 `dest` 在 `src` 内部时拒绝复制（防止递归嵌套的最终防线）
- `migrateDataToNewDirAsync`：遍历旧目录时跳过新目录本身（当新目录在旧目录内部时）
- `migrateDataToNewDirAsync`：当新目录在旧目录内部时，不删除旧目录（避免删除新目录）
- `migrateUserDataIfNeededAsync`：跳过作为新数据目录祖先的旧目录
- `set-data-directory` IPC 处理器：添加前置验证，禁止将数据目录设置到当前数据目录的子目录内

### 新功能

#### 1. 安装时读取之前的数据存储地址

- NSIS 安装程序 `customInit` 宏读取旧版本安装目录（从注册表 `InstallLocation`）
- `customInstall` 宏将旧安装目录写入 `old-install-dir.txt` 标记文件（UTF-16LE 编码）
- 应用启动时 `migrateUserDataIfNeededAsync` 读取标记文件，将旧安装目录下 `data` 文件夹的数据迁移到当前数据目录

#### 2. 设置中选择数据地址

- 设置面板新增数据目录管理功能：
  - 显示当前数据存储位置
  - 更改数据存储位置（选择新目录后自动迁移数据）
  - 打开数据目录（在文件资源管理器中打开）
  - 重置为默认位置

### NSIS 安装程序改进

- `customInit`：强制关闭可能正在运行的应用进程（`taskkill`，正确处理中文文件名）
- `customInit`：删除旧版本注册表项，跳过 `uninstallOldVersion`（避免旧卸载程序的 `CHECK_APP_RUNNING` 因 cmd.exe 编码问题误报进程正在运行）
- `customInstall`：确保安装目录下存在 `data` 目录
- `customUnInstall`：卸载前关闭应用进程，不删除任何用户数据
- `!ifndef BUILD_UNINSTALLER` 守卫：解决 `Var OldInstallDir` 在卸载程序构建中未引用导致的编译错误

### 构建系统改进

- 简化构建脚本：
  - `build:nsis`：仅构建 NSIS 安装版
  - `build:portable`：构建便携版（dir + zip）
  - `build:all`：同时构建安装版和便携版
- 移除 `portable` 单 exe 目标，改用 `dir` + zip 方式生成便携版
- 清理项目结构，移除所有辅助脚本和旧构建输出

### 项目文件清理

删除了以下非核心文件：
- `build_with_icon.js`、`create_ico.js`、`set_icon.js` — 图标辅助脚本
- `check_icons.ps1`、`analyze_ico.ps1` — 图标检查脚本
- `debug_app.js` — 调试脚本
- `README.md`、`便携版测试报告与性能优化说明.md` — 旧文档
- `src/splash.html` — 未使用的启动画面文件（启动画面已内嵌在 main.js 中）

---

## v1.3

### 功能改进

- 优化启动速度，添加启动画面（Splash Window）
- 添加窗口状态记忆（位置、大小、最大化状态）
- 优化图标处理逻辑，验证 ICO 文件格式有效性

---

## v1.2

### 功能改进

- 添加背景图片功能
- 添加提示词预览图片功能
- 添加数据导出/导入功能
- 添加缓存系统提升加载速度

---

## v1.1

### 初始功能

- 提示词分类管理
- 提示词增删改查
- 批量操作
- 搜索功能
- 翻译功能
- 界面主题设置
